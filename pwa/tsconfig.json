{
  "// note": "Type-CHECK only — no emit, no build step. Run: npx tsc -p pwa/tsconfig.json (or point your editor here). Only files carrying a `// @ts-check` comment are checked; everything is still shipped as plain .js via //go:embed.",
  "compilerOptions": {
    "allowJs": true,
    "checkJs": false,
    "noEmit": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "target": "es2020",
    "lib": ["es2020", "dom", "dom.iterable"],
    "strict": false,
    "noImplicitAny": false,
    "strictNullChecks": false,
    "skipLibCheck": true
  },
  "include": ["types.d.ts", "*.js"]
}
